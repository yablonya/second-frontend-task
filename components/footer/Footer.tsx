import styles from '@/components/footer/Footer.module.css'
const Footer = () => {
  return (
    <footer className={styles.wrapper}>
      There could be a cool footer here, but not today
    </footer>
  )
}

export default Footer;